fastapi>=0.115
email-validator>=2.2
uvicorn[standard]>=0.30
sqlalchemy>=2.0
alembic>=1.13
pydantic-settings>=2.4
bcrypt>=4.1
python-jose[cryptography]>=3.3
authlib>=1.3
httpx>=0.27
itsdangerous>=2.2
psycopg[binary]>=3.2
# the admin Runs tab executes questions_processing/llm.py on the server, and
# every provider it offers except Anthropic speaks the OpenAI wire format
openai>=1.40
# the admin Data tab runs the scrapers server-side; they fetch pages and parse
# them in memory, so no HTML is stored
requests>=2.32
beautifulsoup4>=4.12
# the scrapers ask BeautifulSoup for the lxml tree builder by name
lxml>=5.2
